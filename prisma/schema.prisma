generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model JuniorCoreWaitlistEmail {
  email String @id @unique @db.VarChar(50)
}
